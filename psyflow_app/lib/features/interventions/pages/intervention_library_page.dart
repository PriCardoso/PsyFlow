import 'package:flutter/material.dart';

class InterventionLibraryPage
    extends StatelessWidget {
  const InterventionLibraryPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          'Biblioteca de Intervenções',
        ),
      ),
      body: const Center(
        child: Text(
          'Em construção',
        ),
      ),
    );
  }
}